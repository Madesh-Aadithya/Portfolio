Step 1 — Create the Project
Goal

Create a clean React project using Vite.

Why Vite?

Vite is fast, modern, and widely used. It gives you:

Fast development server
Hot Module Replacement (instant updates)
Simple configuration
Excellent React support

1. Open your terminal

Navigate to the folder where you want the project.

Example:

cd Documents
2. Create the project

Run:

npm create vite@latest portfolio-ui

It will ask a few questions:

Project name:

Press Enter (or keep portfolio-ui).

Select a framework:

Choose:

React
Select a variant:

Choose:

JavaScript

(We'll use JavaScript first. If you later want to learn TypeScript, we can migrate.)

3. Enter the project folder
cd portfolio-ui
4. Install dependencies
npm install
5. Start the development server
npm run dev

You should see something like:

Local:

http://localhost:5173/

Open that URL in your browser. If everything worked, you'll see the default Vite + React page.


--------------------------------------------------IF USING UBUNTU SERVER------------------------------------------------------

Method 1: SSH Local Port Forwarding (Recommended & Secure)
This forwards traffic from port 5173 on your local computer to port 5173 on your Ubuntu server over a secure SSH connection.

Keep Vite running on your Ubuntu server.

Open a new terminal/command prompt on your local computer (not on the server).

Run the following SSH tunneling command:

Bash
ssh -L 5173:localhost:5173 madesh@<your-server-ip>
(Replace <your-server-ip> with the actual IP address or domain of your Ubuntu server).

Open your local web browser and navigate to:

Plaintext
http://localhost:5173


Method 2: Expose Vite to your Local Network
By default, Vite listens only on localhost inside Ubuntu. It may work through some forwarding, but later we need proper access.

Stop the server:

Ctrl + C

Now start Vite with host enabled:

npm run dev -- --host 0.0.0.0

You should see something like:

Local:   http://localhost:5173/
Network: http://192.168.x.x:5173/

Try opening the Network URL in your Windows browser.

Example:

http://192.168.1.50:5173

Using VS Code SSH Extension?
If you are connected to your server via VS Code Remote - SSH, VS Code usually handles this automatically:

Go to the Ports tab in the bottom panel (next to Terminal / Output).

Look for port 5173. If it's listed, click the globe icon or right-click to select Open in Browser.

If it isn't listed, click Forward a Port and enter 5173.

-----------------------------------------------------------------------------------------------------------------------------

Step 1.2 — Initialize Git

Inside portfolio-ui:

git init

Create your first checkpoint:

git add .
git commit -m "Initial React Vite setup"

Why?

Because from now on every major step can be saved. If something breaks, you can return.

------------------------IF USING SEPARATE LOCAL REPOSITORY RATHER THAN FIRST PULLING FROM THE GITHUB REPOSITORY--------------
Run this command to set merge as your preference for this pull, then run the pull again:

Bash
git pull origin main --allow-unrelated-histories --no-rebase
After this command completes, finish pushing your code:

Bash
git push -u origin main

------------------------------------------------------------------------------------------------------------------------------


# Step 2 — Install Tailwind CSS

## Goal

Add Tailwind CSS to our React project.

Why Tailwind?

Instead of writing separate CSS files like:

```css
.card {
  border-radius: 20px;
  background: black;
}
```

we write styles directly:

```jsx
<div className="rounded-xl bg-black">
```

Benefits:

* Faster UI development
* Consistent design
* Responsive classes built in
* Used widely in modern React projects

---

## 2.1 Install Tailwind packages

Inside your `portfolio-ui` folder:

```bash
npm install tailwindcss @tailwindcss/vite
```

---

## 2.2 Configure Vite

Open:

```
vite.config.js
```

You currently have something like:

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
})
```

Change it to:

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
  ],
})
```

---

## 2.3 Add Tailwind to CSS

Open:

```
src/index.css
```

Delete everything inside.

Add:

```css
@import "tailwindcss";
```

---

## 2.4 Test Tailwind

Open:

```
src/App.jsx
```

Replace everything with:

```jsx
function App() {
  return (
    <h1 className="text-5xl font-bold text-blue-500">
      My Portfolio
    </h1>
  )
}

export default App
```

---

## 2.5 Restart Vite

Stop the server:

```
Ctrl + C
```

Start again:

```bash
npm run dev -- --host 0.0.0.0
```

Open the browser.

You should see:

* Large text
* Bold font
* Blue color

---

## 2.6 Save progress

If it works:

```bash
git add .
git commit -m "Setup Tailwind CSS"
git push
```

---

Your first real UI technology is now installed.

Next step:
**Step 3 — Clean Vite default files and prepare a professional project structure.**

-----------------------------------------------------------------------------------------------------------------------------

# Step 3 — Clean Vite Default Files & Prepare Project Structure

## Goal

Remove the Vite demo code and create a clean foundation for our portfolio.

Right now Vite created files for learning/demo purposes. We don't need them.

---

## 3.1 Clean `src` folder

Currently you have:

```
src/
 ├── assets/
 │    └── react.svg
 ├── App.jsx
 ├── App.css
 ├── index.css
 └── main.jsx
```

---

## Delete:

Remove:

```
src/assets/react.svg
src/App.css
```

We won't use them.

---

## 3.2 Update `App.jsx`

Open:

```
src/App.jsx
```

Replace everything:

```jsx
function App() {
  return (
    <div>
      <h1 className="text-4xl font-bold">
        My Portfolio
      </h1>
    </div>
  )
}

export default App
```

This is our temporary root component.

---

## 3.3 Clean `index.css`

Open:

```
src/index.css
```

Replace with:

```css
@import "tailwindcss";

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-height: 100vh;
}
```

Explanation:

* `@import "tailwindcss"` → enables Tailwind
* `box-sizing` → makes sizing predictable
* remove browser default margin

---

## 3.4 Create Professional Folder Structure

Inside `src`, create:

```
src/

├── assets/
│
├── components/
│   ├── common/
│   ├── layout/
│   └── sections/
│
├── pages/
│
├── admin/
│
├── hooks/
│
├── utils/
│
├── routes/
│
├── styles/
│
├── App.jsx
├── main.jsx
└── index.css
```

---

## What each folder is for

### `components/`

Reusable UI pieces.

Example:

```
components/
 ├── Navbar
 ├── Button
 ├── Card
 └── ProjectCard
```

Used in multiple pages.

---

### `components/layout/`

Big page structures.

Example:

```
Navbar
Footer
MainLayout
```

---

### `components/sections/`

Portfolio sections.

Example:

```
Hero
About
Skills
Projects
Contact
```

---

### `pages/`

Full pages.

Example:

```
Home.jsx
Projects.jsx
Certificates.jsx
```

---

### `admin/`

Your private dashboard.

Example:

```
admin/
 ├── Login
 ├── Dashboard
 ├── ManageProjects
```

---

### `hooks/`

Custom React logic.

Example:

```
useFetch.js
useAuth.js
```

---

### `utils/`

Helper functions.

Example:

```
formatDate.js
api.js
```

---

### `routes/`

All navigation rules.

Example:

```
public routes
admin routes
```

---

### `styles/`

Extra CSS if needed later.

---

## 3.5 Check your project

Your final structure should look like:

```
portfolio-ui

src
│
├── assets
├── components
│   ├── common
│   ├── layout
│   └── sections
│
├── pages
├── admin
├── hooks
├── utils
├── routes
├── styles
│
├── App.jsx
├── main.jsx
└── index.css
```

---

## 3.6 Commit this step

```bash
git add .
git commit -m "Clean Vite files and create project structure"
git push
```

---

## What you learned

You now have the same type of structure used in production React projects:

* Separation of UI
* Separation of pages
* Space for admin panel
* Space for backend integration later

---

Next step:

**Step 4 — Install React Router and create the basic page navigation system.**

-----------------------------------------------------------------------------------------------------------------------------

Keeping everything on **one page** is a good choice for a portfolio. The navbar can simply scroll to each section (Hero, About, Skills, Projects, etc.), while the admin panel will be on separate routes later.

So our routes will be:

```text
/          → Portfolio (single-page)
/admin     → Login
/dashboard → Admin Dashboard
```

---

# Step 4 — Install React Router

## 4.1 Install it

```bash
npm install react-router-dom
```

---

## 4.2 Create pages

Inside `src/pages/`, create:

```text
Home.jsx
```

`Home.jsx`

```jsx
function Home() {
  return (
    <main>
      <h1 className="text-4xl font-bold">Home Page</h1>
    </main>
  );
}

export default Home;
```

---

## 4.3 Update `App.jsx`

Replace it with:

```jsx
import { BrowserRouter, Routes, Route } from "react-router-dom";
import Home from "./pages/Home";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Home />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

---

## Why use React Router if everything is on one page?

Even though the portfolio is a single page, we'll later add:

* `/admin`
* `/dashboard`

without changing the project structure.

---

## Commit

```bash
git add .
git commit -m "Setup React Router"
git push
```

✅ At this point, visiting `http://localhost:5173/` should display **Home Page**.

**Next step:** We'll create the **MainLayout** (Navbar + Footer + content area), which every public page will use. This keeps the project organized and makes adding sections much easier.

------------------------------------------------------------------------------------------------------------------------------

# Step 5 — Create the Main Layout

Since your portfolio is **one page**, we'll create one layout that contains:

```text
Navbar
   ↓
Portfolio Sections
   ↓
Footer
```

We won't build the actual sections yet. First, we'll create the structure.

---

## 5.1 Create `MainLayout.jsx`

Create:

```text
src/components/layout/MainLayout.jsx
```

Add:

```jsx
function MainLayout({ children }) {
  return (
    <div className="min-h-screen">
      <header>
        Navbar will go here
      </header>

      <main>
        {children}
      </main>

      <footer>
        Footer will go here
      </footer>
    </div>
  );
}

export default MainLayout;
```

### What is `children`?

When we write:

```jsx
<MainLayout>
  <Home />
</MainLayout>
```

React puts `<Home />` into:

```jsx
{children}
```

So `MainLayout` becomes a reusable wrapper.

---

## 5.2 Update `App.jsx`

Change it to:

```jsx
import { BrowserRouter, Routes, Route } from "react-router-dom";
import Home from "./pages/Home";
import MainLayout from "./components/layout/MainLayout";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route
          path="/"
          element={
            <MainLayout>
              <Home />
            </MainLayout>
          }
        />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

---

## 5.3 Update `Home.jsx`

For now, make it:

```jsx
function Home() {
  return (
    <section className="min-h-screen flex items-center justify-center">
      <h1 className="text-4xl font-bold">
        My Portfolio
      </h1>
    </section>
  );
}

export default Home;
```

---

## 5.4 Test it

Run:

```bash
npm run dev -- --host 0.0.0.0
```

You should see:

```text
My Portfolio
```

The important thing is that your application now has this hierarchy:

```text
App
 │
 └── BrowserRouter
      │
      └── Route /
           │
           └── MainLayout
                │
                ├── Header
                ├── Home
                └── Footer
```

---

## 5.5 Commit

If everything works:

```bash
git add .
git commit -m "Create main portfolio layout"
git push
```

### Next step

**Step 6 — Build the Navbar.**

We'll make it scroll smoothly to sections like:

`About` → `Skills` → `Projects` → `Certifications` → `Contact`

No separate pages for those.


------------------------------------------------------------------------------------------------------------------------------

# Step 6 — Build the Navbar

Now we'll create the navigation bar for our **single-page portfolio**.

The navbar will eventually look like:

```text
┌──────────────────────────────────────────────────────────────┐
│ Madesh       About  Skills  Projects  Certificates  Contact │
└──────────────────────────────────────────────────────────────┘
```

For now, we'll only make the navigation work. We'll style it properly later.

---

## 6.1 Create `Navbar.jsx`

Create:

```text
src/components/layout/Navbar.jsx
```

Add:

```jsx
function Navbar() {
  const navItems = [
    { name: "About", id: "about" },
    { name: "Skills", id: "skills" },
    { name: "Projects", id: "projects" },
    { name: "Certificates", id: "certificates" },
    { name: "Contact", id: "contact" },
  ];

  const handleScroll = (id) => {
    document.getElementById(id)?.scrollIntoView({
      behavior: "smooth",
    });
  };

  return (
    <nav>
      <button onClick={() => handleScroll("home")}>
        Madesh
      </button>

      {navItems.map((item) => (
        <button
          key={item.id}
          onClick={() => handleScroll(item.id)}
        >
          {item.name}
        </button>
      ))}
    </nav>
  );
}

export default Navbar;
```

### What's happening here?

This:

```jsx
document.getElementById(id)?.scrollIntoView({
  behavior: "smooth",
});
```

finds a section by its `id` and smoothly scrolls to it.

For example:

```html
<section id="projects">
```

can be reached with:

```jsx
handleScroll("projects");
```

---

# 6.2 Add Navbar to `MainLayout`

Open:

```text
src/components/layout/MainLayout.jsx
```

Change it to:

```jsx
import Navbar from "./Navbar";

function MainLayout({ children }) {
  return (
    <div className="min-h-screen">
      <header>
        <Navbar />
      </header>

      <main>
        {children}
      </main>

      <footer>
        Footer will go here
      </footer>
    </div>
  );
}

export default MainLayout;
```

---

# 6.3 Give Home the `home` ID

Change `Home.jsx`:

```jsx
function Home() {
  return (
    <section
      id="home"
      className="min-h-screen flex items-center justify-center"
    >
      <h1 className="text-4xl font-bold">
        My Portfolio
      </h1>
    </section>
  );
}

export default Home;
```

---

# 6.4 Test

Start Vite:

```bash
npm run dev -- --host 0.0.0.0
```

You should see the navigation buttons.

At the moment, clicking:

```text
About
Skills
Projects
Certificates
Contact
```

won't do anything because those sections don't exist yet.

That's expected.

We'll create those sections next.

---

# 6.5 Commit

```bash
git add .
git commit -m "Create single page portfolio navbar"
git push
```

### One important decision

We're **not** going to create five separate React pages for your portfolio information.

We'll eventually have:

```text
Home
│
├── Hero
├── About
├── Skills
├── Projects
├── Certificates
├── Contact
└── Footer
```

all rendered on `/`.

The **admin panel** remains separate:

```text
/admin
```

That's a clean architecture for what you're trying to build.

**Next: Step 7 — Create the portfolio sections with placeholder content and make the navbar scrolling actually work.**

------------------------------------------------------------------------------------------------------------------------------

# Step 7 — Create the Portfolio Sections

Now we'll create the sections that make up your **single-page portfolio**.

Don't worry about making them beautiful yet. We're only creating the **structure**.

---

## 7.1 Create the section files

Inside:

```text
src/components/sections/
```

create:

```text
Hero.jsx
About.jsx
Skills.jsx
Projects.jsx
Certificates.jsx
Contact.jsx
```

Your structure becomes:

```text
src/
└── components/
    └── sections/
        ├── Hero.jsx
        ├── About.jsx
        ├── Skills.jsx
        ├── Projects.jsx
        ├── Certificates.jsx
        └── Contact.jsx
```

---

# 7.2 Create the sections

### `Hero.jsx`

```jsx
function Hero() {
  return (
    <section
      id="home"
      className="min-h-screen flex items-center justify-center"
    >
      <h1 className="text-5xl font-bold">
        Hello, I'm Madesh
      </h1>
    </section>
  );
}

export default Hero;
```

### `About.jsx`

```jsx
function About() {
  return (
    <section id="about" className="min-h-screen p-10">
      <h2 className="text-4xl font-bold">About Me</h2>
    </section>
  );
}

export default About;
```

### `Skills.jsx`

```jsx
function Skills() {
  return (
    <section id="skills" className="min-h-screen p-10">
      <h2 className="text-4xl font-bold">Skills</h2>
    </section>
  );
}

export default Skills;
```

### `Projects.jsx`

```jsx
function Projects() {
  return (
    <section id="projects" className="min-h-screen p-10">
      <h2 className="text-4xl font-bold">Projects</h2>
    </section>
  );
}

export default Projects;
```

### `Certificates.jsx`

```jsx
function Certificates() {
  return (
    <section id="certificates" className="min-h-screen p-10">
      <h2 className="text-4xl font-bold">Certificates</h2>
    </section>
  );
}

export default Certificates;
```

### `Contact.jsx`

```jsx
function Contact() {
  return (
    <section id="contact" className="min-h-screen p-10">
      <h2 className="text-4xl font-bold">Contact</h2>
    </section>
  );
}

export default Contact;
```

---

# 7.3 Put them inside `Home.jsx`

Now `Home.jsx` becomes the page that combines everything.

```jsx
import Hero from "../components/sections/Hero";
import About from "../components/sections/About";
import Skills from "../components/sections/Skills";
import Projects from "../components/sections/Projects";
import Certificates from "../components/sections/Certificates";
import Contact from "../components/sections/Contact";

function Home() {
  return (
    <>
      <Hero />
      <About />
      <Skills />
      <Projects />
      <Certificates />
      <Contact />
    </>
  );
}

export default Home;
```

---

# 7.4 Test it

Run:

```bash
npm run dev -- --host 0.0.0.0
```

Now you should be able to click:

```text
About
Skills
Projects
Certificates
Contact
```

and the page should smoothly scroll to each section.

You now have the basic architecture:

```text
App
│
└── MainLayout
    │
    ├── Navbar
    │
    └── Home
        │
        ├── Hero
        ├── About
        ├── Skills
        ├── Projects
        ├── Certificates
        └── Contact
```

That's an important milestone. **The basic frontend architecture is now in place.**

---

## 7.5 Commit

```bash
git add .
git commit -m "Create portfolio page sections"
git push
```

### Next step

**Step 8 — Build the global theme.**

We'll decide the actual **dark background, text colors, accent color, typography, spacing, and overall visual language** before we start making the Hero section look professional.

------------------------------------------------------------------------------------------------------------------------------

## Step 8 — Create the Global Theme

Now we'll set the **visual foundation** of the portfolio before building the actual sections.

We'll keep this step small: **colors + background + smooth scrolling**.

### 8.1 Replace `src/index.css`

Replace everything with:

```css
@import "tailwindcss";

html {
  scroll-behavior: smooth;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-width: 320px;
  min-height: 100vh;
  background: #0a0a0a;
  color: #ffffff;
  font-family: Arial, Helvetica, sans-serif;
}
```

### What we just did

```css
background: #0a0a0a;
```

Gives us the dark portfolio background.

```css
color: #ffffff;
```

Makes the default text white.

```css
scroll-behavior: smooth;
```

Makes the navbar scrolling smooth.

---

### 8.2 Test it

Run:

```bash
npm run dev -- --host 0.0.0.0
```

Your page should now have a **dark background** with white text.

Don't worry about the design looking basic yet. We're going to build the actual visual design section by section.

### 8.3 Commit

```bash
git add .
git commit -m "Create global portfolio theme"
git push
```

That's it for Step 8.

**Next: Step 9 — Build the Navbar properly**: dark glass effect, spacing, hover animations, responsive behavior, and the portfolio name.


------------------------------------------------------------------------------------------------------------------------------

# Step 9 — Build the Navbar

Now we'll turn our basic navbar into a proper modern portfolio navbar.

We'll keep this step focused only on the **desktop navbar**. Mobile navigation comes later.

---

## 9.1 Replace `Navbar.jsx`

Open:

```text
src/components/layout/Navbar.jsx
```

Replace the entire file with:

```jsx
function Navbar() {
  const navItems = [
    { name: "About", id: "about" },
    { name: "Skills", id: "skills" },
    { name: "Projects", id: "projects" },
    { name: "Certificates", id: "certificates" },
    { name: "Contact", id: "contact" },
  ];

  const handleScroll = (id) => {
    document.getElementById(id)?.scrollIntoView({
      behavior: "smooth",
    });
  };

  return (
    <nav className="fixed top-0 left-0 z-50 w-full border-b border-white/10 bg-black/70 backdrop-blur-lg">
      <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        
        {/* Logo */}
        <button
          onClick={() => handleScroll("home")}
          className="text-xl font-bold tracking-tight"
        >
          Madesh
        </button>

        {/* Navigation */}
        <div className="flex items-center gap-6">
          {navItems.map((item) => (
            <button
              key={item.id}
              onClick={() => handleScroll(item.id)}
              className="text-sm text-white/70 transition hover:text-white"
            >
              {item.name}
            </button>
          ))}
        </div>

      </div>
    </nav>
  );
}

export default Navbar;
```

---

## 9.2 Why these classes?

You don't need to memorize them.

For example:

```text
fixed
```

keeps the navbar at the top while scrolling.

```text
bg-black/70
```

means black with 70% opacity.

```text
backdrop-blur-lg
```

creates the glass effect.

```text
border-b border-white/10
```

adds a subtle bottom border.

```text
transition hover:text-white
```

makes the text smoothly brighten when you hover.

---

## 9.3 Test it

Refresh the browser.

You should now have something like:

```text
┌──────────────────────────────────────────────────────────┐
│ Madesh        About Skills Projects Certificates Contact │
└──────────────────────────────────────────────────────────┘
│                                                          │
│                  Hello, I'm Madesh                       │
│                                                          │
```

Scroll down.

The navbar should remain at the top.

---

## 9.4 One small issue

Our Hero currently starts at the very top of the page, so the fixed navbar can cover some content.

We'll solve that properly when we build the Hero section.

**Don't change anything else yet.**

---

## 9.5 Commit

```bash
git add .
git commit -m "Build modern portfolio navbar"
git push
```

That's Step 9.

**Next → Step 10: Build the Hero section.** This is where the portfolio starts looking like an actual professional website.


------------------------------------------------------------------------------------------------------------------------------

# Step 10 — Build the Hero Section

Now we're going to build the **first thing visitors see**.

For now, we'll focus only on the structure and content. We'll add the more advanced animations later.

The Hero will have:

```text
Hi, I'm Madesh
Aspiring DevOps / Cloud Engineer

Short introduction

[View Projects] [Contact Me]
```

---

## 10.1 Replace `Hero.jsx`

Open:

```text
src/components/sections/Hero.jsx
```

Replace everything with:

```jsx
function Hero() {
  const scrollToSection = (id) => {
    document.getElementById(id)?.scrollIntoView({
      behavior: "smooth",
    });
  };

  return (
    <section
      id="home"
      className="flex min-h-screen items-center justify-center px-6 pt-20"
    >
      <div className="mx-auto max-w-4xl text-center">

        <p className="mb-4 text-sm font-medium uppercase tracking-[0.3em] text-white/50">
          Welcome to my portfolio
        </p>

        <h1 className="text-5xl font-bold tracking-tight sm:text-6xl md:text-7xl">
          Hi, I'm Madesh.
        </h1>

        <h2 className="mt-4 text-2xl font-semibold text-white/70 sm:text-3xl">
          Aspiring DevOps & Cloud Engineer
        </h2>

        <p className="mx-auto mt-6 max-w-2xl text-base leading-7 text-white/50 sm:text-lg">
          I build, automate, and deploy applications while continuously
          learning about Linux, cloud infrastructure, containers, and DevOps.
        </p>

        <div className="mt-8 flex flex-col justify-center gap-4 sm:flex-row">

          <button
            onClick={() => scrollToSection("projects")}
            className="rounded-xl bg-white px-6 py-3 font-medium text-black transition hover:bg-white/90"
          >
            View Projects
          </button>

          <button
            onClick={() => scrollToSection("contact")}
            className="rounded-xl border border-white/15 px-6 py-3 font-medium text-white transition hover:bg-white/10"
          >
            Contact Me
          </button>

        </div>

      </div>
    </section>
  );
}

export default Hero;
```

---

# 10.2 What we created

The structure is now:

```text
Hero
│
├── Small introduction
│
├── Main heading
│
├── Professional title
│
├── Description
│
└── Buttons
    ├── View Projects
    └── Contact Me
```

The important part is that the buttons already interact with our single-page navigation.

For example:

```jsx
scrollToSection("projects")
```

finds:

```html
<section id="projects">
```

and scrolls there.

---

# 10.3 Check the browser

You should now see something approximately like:

```text
              WELCOME TO MY PORTFOLIO

                 Hi, I'm Madesh.

          Aspiring DevOps & Cloud Engineer

       I build, automate, and deploy applications
       while continuously learning about Linux...

             [ View Projects ] [ Contact Me ]
```

It will still look relatively simple.

**That's intentional.**

We're building the foundation first. Later we'll add:

* gradient effects
* animated background
* Framer Motion
* better typography
* visual elements
* subtle glowing effects

---

# 10.4 Commit

If everything works:

```bash
git add .
git commit -m "Build portfolio hero section"
git push
```

That's Step 10.

**Next → Step 11: Build the About section.**

------------------------------------------------------------------------------------------------------------------------------

# Step 11 — Build the About Section

Now we'll create the **About** section.

Keep it simple for now. The goal is to establish the structure before we make it visually polished.

## 11.1 Replace `About.jsx`

Open:

```text
src/components/sections/About.jsx
```

Replace everything with:

```jsx
function About() {
  return (
    <section
      id="about"
      className="px-6 py-24"
    >
      <div className="mx-auto max-w-6xl">

        <p className="mb-3 text-sm font-medium uppercase tracking-[0.3em] text-white/50">
          About Me
        </p>

        <h2 className="text-4xl font-bold tracking-tight sm:text-5xl">
          Building my skills through real projects.
        </h2>

        <div className="mt-8 max-w-3xl space-y-5 text-base leading-7 text-white/60 sm:text-lg">
          <p>
            I'm a developer interested in DevOps, cloud infrastructure,
            Linux, automation, and backend technologies.
          </p>

          <p>
            I enjoy learning by building practical projects and
            understanding how applications work from development to
            deployment.
          </p>

          <p>
            This portfolio documents my projects, technical skills,
            certifications, and the technologies I'm learning.
          </p>
        </div>

      </div>
    </section>
  );
}

export default About;
```

---

## 11.2 What we're doing here

We're deliberately keeping your information separate from the visual components.

For example:

```jsx
<h2>
  Building my skills through real projects.
</h2>
```

is content.

Later, when we connect the backend, this can become something like:

```text
Database
    ↓
Express API
    ↓
React
    ↓
About Section
```

So eventually you won't need to edit the React component just to change your bio.

That's important for the **admin dashboard** we're going to build later.

---

## 11.3 Test the navbar

Click:

```text
About
```

It should smoothly scroll to this section.

You should now have:

```text
Hero
  ↓
About
  ↓
Skills
  ↓
Projects
  ↓
Certificates
  ↓
Contact
```

---

## 11.4 Commit

```bash
git add .
git commit -m "Build about section"
git push
```

That's it for Step 11.

**Next → Step 12: Build the Skills section.**

------------------------------------------------------------------------------------------------------------------------------

# Step 12 — Build the Skills Section

Now we'll create the **Skills** section.

For now, we'll hard-code a few skills. Later, the admin dashboard will let you add/remove/edit them without touching the frontend code.

## 12.1 Replace `Skills.jsx`

Open:

```text
src/components/sections/Skills.jsx
```

Replace everything with:

```jsx
function Skills() {
  const skills = [
    "Linux",
    "Git & GitHub",
    "Docker",
    "Nginx",
    "Networking",
    "Python",
    "JavaScript",
    "React",
    "Node.js",
    "Cloud",
  ];

  return (
    <section
      id="skills"
      className="px-6 py-24"
    >
      <div className="mx-auto max-w-6xl">

        <p className="mb-3 text-sm font-medium uppercase tracking-[0.3em] text-white/50">
          Skills
        </p>

        <h2 className="text-4xl font-bold tracking-tight sm:text-5xl">
          Technologies I work with.
        </h2>

        <div className="mt-10 grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-5">
          {skills.map((skill) => (
            <div
              key={skill}
              className="rounded-xl border border-white/10 bg-white/5 p-5 text-center transition hover:-translate-y-1 hover:bg-white/10"
            >
              <span className="font-medium">
                {skill}
              </span>
            </div>
          ))}
        </div>

      </div>
    </section>
  );
}

export default Skills;
```

## 12.2 What's new?

This is important:

```jsx
const skills = [
  "Linux",
  "Git & GitHub",
  "Docker",
  ...
];
```

We're storing the skills in an **array**.

Then:

```jsx
skills.map((skill) => (
```

creates a card for every skill.

So instead of writing 10 cards manually, React generates them for us.

For example, adding:

```jsx
"Terraform",
```

to the array automatically creates another card.

---

## 12.3 Test it

Click **Skills** in your navbar.

You should see something like:

```text
SKILLS

Technologies I work with.

┌────────┐ ┌────────┐ ┌────────┐
│ Linux  │ │  Git   │ │ Docker │
└────────┘ └────────┘ └────────┘

┌────────┐ ┌────────┐ ┌────────┐
│ Nginx  │ │ Python │ │ React  │
└────────┘ └────────┘ └────────┘
```

The grid automatically adjusts for different screen sizes.

---

## 12.4 Commit

```bash
git add .
git commit -m "Build skills section"
git push
```

That's Step 12.

**Next → Step 13: Build the Projects section.** This one will be slightly more interesting because we'll create reusable `ProjectCard` components.

------------------------------------------------------------------------------------------------------------------------------

# Step 13 — Build the Projects Section

Now we'll make the **Projects** section.

This is where we'll introduce an important React concept: **reusable components**.

Instead of putting all the project card code inside `Projects.jsx`, we'll create a separate `ProjectCard`.

---

## 13.1 Create `ProjectCard.jsx`

Create:

```text
src/components/common/ProjectCard.jsx
```

Add:

```jsx
function ProjectCard({ project }) {
  return (
    <article className="rounded-2xl border border-white/10 bg-white/5 p-6 transition hover:-translate-y-1 hover:bg-white/10">
      <div className="flex items-start justify-between gap-4">
        <h3 className="text-xl font-semibold">
          {project.title}
        </h3>

        <span className="text-sm text-white/40">
          {project.year}
        </span>
      </div>

      <p className="mt-4 leading-7 text-white/60">
        {project.description}
      </p>

      <div className="mt-5 flex flex-wrap gap-2">
        {project.technologies.map((technology) => (
          <span
            key={technology}
            className="rounded-full border border-white/10 px-3 py-1 text-xs text-white/60"
          >
            {technology}
          </span>
        ))}
      </div>

      <div className="mt-6 flex gap-4">
        {project.github && (
          <a
            href={project.github}
            target="_blank"
            rel="noreferrer"
            className="text-sm font-medium text-white/70 transition hover:text-white"
          >
            GitHub →
          </a>
        )}

        {project.demo && (
          <a
            href={project.demo}
            target="_blank"
            rel="noreferrer"
            className="text-sm font-medium text-white/70 transition hover:text-white"
          >
            Live Demo →
          </a>
        )}
      </div>
    </article>
  );
}

export default ProjectCard;
```

### Why create a separate component?

Because every project has the same structure:

```text
Project
├── Title
├── Year
├── Description
├── Technologies
└── Links
```

So we can reuse:

```jsx
<ProjectCard project={project} />
```

for every project.

---

# 13.2 Replace `Projects.jsx`

Open:

```text
src/components/sections/Projects.jsx
```

Replace everything with:

```jsx
import ProjectCard from "../common/ProjectCard";

function Projects() {
  const projects = [
    {
      title: "Portfolio Website",
      year: "2026",
      description:
        "A modern personal portfolio built with React, Tailwind CSS, and an Express backend.",
      technologies: ["React", "Tailwind CSS", "Node.js"],
      github: "https://github.com/",
      demo: "",
    },
    {
      title: "Static Website Deployment",
      year: "2026",
      description:
        "A DevOps project focused on containerizing a website and deploying it with Docker and Nginx.",
      technologies: ["Linux", "Docker", "Nginx", "Git"],
      github: "https://github.com/",
      demo: "",
    },
  ];

  return (
    <section
      id="projects"
      className="px-6 py-24"
    >
      <div className="mx-auto max-w-6xl">

        <p className="mb-3 text-sm font-medium uppercase tracking-[0.3em] text-white/50">
          Projects
        </p>

        <h2 className="text-4xl font-bold tracking-tight sm:text-5xl">
          Things I've built.
        </h2>

        <div className="mt-10 grid gap-6 md:grid-cols-2">
          {projects.map((project) => (
            <ProjectCard
              key={project.title}
              project={project}
            />
          ))}
        </div>

      </div>
    </section>
  );
}

export default Projects;
```

---

## 13.3 Understand the data

This part:

```jsx
const projects = [
  {
    title: "Portfolio Website",
    year: "2026",
    description: "...",
    technologies: ["React", "Tailwind CSS", "Node.js"],
    github: "...",
    demo: "",
  },
];
```

is **project data**.

And this:

```jsx
<ProjectCard project={project} />
```

is the **UI component**.

That's a pattern we'll use heavily later.

Eventually:

```text
Admin Dashboard
       ↓
Express API
       ↓
Project data
       ↓
Projects.jsx
       ↓
ProjectCard
```

So when you add a project through your admin panel, the public portfolio can automatically display it.

---

## 13.4 Test

Click:

**Projects**

You should now see two project cards.

The technologies should appear as small tags.

---

## 13.5 One thing to change

Don't worry about the GitHub URLs yet.

We used:

```text
https://github.com/
```

only as placeholders.

We'll replace them with your actual project links when your projects are ready.

---

## 13.6 Commit

```bash
git add .
git commit -m "Build reusable project cards"
git push
```

That's Step 13.

**Next → Step 14: Build the Certifications section.**

------------------------------------------------------------------------------------------------------------------------------

# Step 14 — Build the Certifications Section

This section will display your certifications in reusable cards.

We'll use the same idea from the Projects section: **data + reusable component**.

---

## 14.1 Create `CertificateCard.jsx`

Create:

```text
src/components/common/CertificateCard.jsx
```

Add:

```jsx
function CertificateCard({ certificate }) {
  return (
    <article className="rounded-2xl border border-white/10 bg-white/5 p-6 transition hover:-translate-y-1 hover:bg-white/10">
      <div className="flex items-start justify-between gap-4">
        <div>
          <h3 className="text-xl font-semibold">
            {certificate.name}
          </h3>

          <p className="mt-2 text-sm text-white/50">
            {certificate.issuer}
          </p>
        </div>

        <span className="text-sm text-white/40">
          {certificate.year}
        </span>
      </div>

      <p className="mt-4 leading-7 text-white/60">
        {certificate.description}
      </p>

      {certificate.link && (
        <a
          href={certificate.link}
          target="_blank"
          rel="noreferrer"
          className="mt-6 inline-block text-sm font-medium text-white/70 transition hover:text-white"
        >
          View Certificate →
        </a>
      )}
    </article>
  );
}

export default CertificateCard;
```

---

# 14.2 Replace `Certificates.jsx`

Open:

```text
src/components/sections/Certificates.jsx
```

Replace everything with:

```jsx
import CertificateCard from "../common/CertificateCard";

function Certificates() {
  const certificates = [
    {
      name: "Your Certification",
      issuer: "Certification Provider",
      year: "2026",
      description:
        "A short description of what this certification covers.",
      link: "",
    },
    {
      name: "Another Certification",
      issuer: "Certification Provider",
      year: "2026",
      description:
        "A short description of what this certification covers.",
      link: "",
    },
  ];

  return (
    <section
      id="certificates"
      className="px-6 py-24"
    >
      <div className="mx-auto max-w-6xl">

        <p className="mb-3 text-sm font-medium uppercase tracking-[0.3em] text-white/50">
          Certifications
        </p>

        <h2 className="text-4xl font-bold tracking-tight sm:text-5xl">
          Certifications & learning.
        </h2>

        <div className="mt-10 grid gap-6 md:grid-cols-2">
          {certificates.map((certificate) => (
            <CertificateCard
              key={`${certificate.name}-${certificate.year}`}
              certificate={certificate}
            />
          ))}
        </div>

      </div>
    </section>
  );
}

export default Certificates;
```

---

## 14.3 Understand what we did

The data:

```jsx
const certificates = [
  {
    name: "Your Certification",
    issuer: "Certification Provider",
    year: "2026",
  },
];
```

gets passed into:

```jsx
<CertificateCard certificate={certificate} />
```

Then the card accesses it through:

```jsx
certificate.name
certificate.issuer
certificate.year
```

This separation will become very useful when we connect the backend.

---

## 14.4 Test

Click:

**Certificates**

You should see two temporary certification cards.

They're placeholders for now.

**Don't put fake certifications on your real portfolio.** We'll replace these with your actual certifications later.

---

## 14.5 Commit

```bash
git add .
git commit -m "Build certifications section"
git push
```

That's Step 14.

**Next → Step 15: Build the Contact section.**

------------------------------------------------------------------------------------------------------------------------------

# Step 15 — Build the Contact Section

Now we'll create the final main section: **Contact**.

For now, this is only the frontend UI. **Don't connect a form to an email service or backend yet.** We'll handle that when we build the backend.

## 15.1 Replace `Contact.jsx`

Open:

```text
src/components/sections/Contact.jsx
```

Replace everything with:

```jsx
function Contact() {
  return (
    <section
      id="contact"
      className="px-6 py-24"
    >
      <div className="mx-auto max-w-6xl">

        <p className="mb-3 text-sm font-medium uppercase tracking-[0.3em] text-white/50">
          Contact
        </p>

        <h2 className="text-4xl font-bold tracking-tight sm:text-5xl">
          Let's connect.
        </h2>

        <p className="mt-5 max-w-2xl text-base leading-7 text-white/60 sm:text-lg">
          If you'd like to discuss a project, opportunity, or just
          connect, feel free to reach out.
        </p>

        <div className="mt-10 grid gap-6 md:grid-cols-2">

          {/* Contact Information */}
          <div className="rounded-2xl border border-white/10 bg-white/5 p-6">
            <h3 className="text-xl font-semibold">
              Get in touch
            </h3>

            <div className="mt-6 space-y-4 text-white/60">
              <p>
                Email: your-email@example.com
              </p>

              <p>
                GitHub: github.com/yourusername
              </p>

              <p>
                LinkedIn: linkedin.com/in/yourusername
              </p>
            </div>
          </div>

          {/* Contact Form */}
          <form className="rounded-2xl border border-white/10 bg-white/5 p-6">

            <div>
              <label
                htmlFor="name"
                className="text-sm text-white/60"
              >
                Name
              </label>

              <input
                id="name"
                type="text"
                placeholder="Your name"
                className="mt-2 w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 text-white outline-none placeholder:text-white/30 focus:border-white/30"
              />
            </div>

            <div className="mt-5">
              <label
                htmlFor="email"
                className="text-sm text-white/60"
              >
                Email
              </label>

              <input
                id="email"
                type="email"
                placeholder="you@example.com"
                className="mt-2 w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 text-white outline-none placeholder:text-white/30 focus:border-white/30"
              />
            </div>

            <div className="mt-5">
              <label
                htmlFor="message"
                className="text-sm text-white/60"
              >
                Message
              </label>

              <textarea
                id="message"
                rows="5"
                placeholder="Write your message..."
                className="mt-2 w-full resize-none rounded-xl border border-white/10 bg-black/40 px-4 py-3 text-white outline-none placeholder:text-white/30 focus:border-white/30"
              />
            </div>

            <button
              type="submit"
              className="mt-6 w-full rounded-xl bg-white px-6 py-3 font-medium text-black transition hover:bg-white/90"
            >
              Send Message
            </button>

          </form>

        </div>

      </div>
    </section>
  );
}

export default Contact;
```

## 15.2 Important: the form doesn't work yet

If you click:

**Send Message**

nothing useful will happen.

That's intentional.

Right now:

```text
Browser
   ↓
Contact Form
   ↓
❌ No backend yet
```

Later we'll build:

```text
Browser
   ↓
React Form
   ↓
Express API
   ↓
Validation
   ↓
Email / Database
```

So don't try to connect it to anything yet.

---

## 15.3 Replace the placeholder contact information

For now, you can leave:

```text
your-email@example.com
github.com/yourusername
linkedin.com/in/yourusername
```

We'll replace them with your actual information when we make the portfolio data configurable.

---

## 15.4 Test

Click **Contact** in the navbar.

You should see:

```text
Contact

Let's connect.

┌─────────────────────┐ ┌────────────────────────┐
│ Get in touch        │ │ Name                   │
│                     │ │ [________________]     │
│ Email               │ │                        │
│ GitHub              │ │ Email                  │
│ LinkedIn            │ │ [________________]     │
│                     │ │                        │
│                     │ │ Message                │
│                     │ │ [________________]     │
│                     │ │                        │
│                     │ │ [ Send Message ]       │
└─────────────────────┘ └────────────────────────┘
```

---

## 15.5 Commit

```bash
git add .
git commit -m "Build contact section"
git push
```

That's **Step 15**.

At this point, we have all the major portfolio sections:

```text
Hero
 ↓
About
 ↓
Skills
 ↓
Projects
 ↓
Certifications
 ↓
Contact
```

**Next → Step 16: Build the Footer and finish the basic public-page structure.**


------------------------------------------------------------------------------------------------------------------------------

# Step 16 — Build the Footer

Now we'll finish the **basic public portfolio structure** with a footer.

This step is small. We're only creating the footer; styling and animations will come later.

---

## 16.1 Create `Footer.jsx`

Create:

```text
src/components/layout/Footer.jsx
```

Add:

```jsx
function Footer() {
  const currentYear = new Date().getFullYear();

  return (
    <footer className="border-t border-white/10 px-6 py-8">
      <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-4 text-sm text-white/40 sm:flex-row">

        <p>
          © {currentYear} Madesh. All rights reserved.
        </p>

        <p>
          Built with React & Tailwind CSS.
        </p>

      </div>
    </footer>
  );
}

export default Footer;
```

### Small thing to notice

Instead of writing:

```jsx
© 2026
```

we use:

```jsx
new Date().getFullYear()
```

That means the year updates automatically.

---

# 16.2 Add Footer to `MainLayout`

Open:

```text
src/components/layout/MainLayout.jsx
```

Replace it with:

```jsx
import Navbar from "./Navbar";
import Footer from "./Footer";

function MainLayout({ children }) {
  return (
    <div className="min-h-screen">
      <header>
        <Navbar />
      </header>

      <main>
        {children}
      </main>

      <Footer />
    </div>
  );
}

export default MainLayout;
```

---

# 16.3 Test

Scroll all the way to the bottom.

You should see:

```text
────────────────────────────────────────

© 2026 Madesh. All rights reserved.

              Built with React & Tailwind CSS.
```

---

# 16.4 Commit

```bash
git add .
git commit -m "Add portfolio footer"
git push
```

---

## Current architecture

You've now built the complete basic public page:

```text
App
│
└── BrowserRouter
    │
    └── /
        │
        └── MainLayout
            │
            ├── Navbar
            │
            ├── Home
            │   ├── Hero
            │   ├── About
            │   ├── Skills
            │   ├── Projects
            │   ├── Certifications
            │   └── Contact
            │
            └── Footer
```

That's a solid foundation.

### Next → Step 17

We'll stop adding sections and start **making the existing portfolio look genuinely modern**.

First up: **proper typography, spacing, accent colors, and visual hierarchy**.
